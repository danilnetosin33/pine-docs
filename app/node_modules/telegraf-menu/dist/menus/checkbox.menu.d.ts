import { GenericMenu } from '../generic-menu';
import { CheckboxConfig, DefaultCtx, MenuOption } from '../interfaces';
import { KeyboardButton } from '../keyboard-button';
export declare class CheckboxMenu<TCtx extends DefaultCtx = DefaultCtx, TState extends string[] = string[], TValue extends string = string> extends GenericMenu<TCtx, TState, TValue> {
    private config;
    constructor(config: CheckboxConfig<TCtx, TState, TValue>);
    stateToMenu(state: TState): KeyboardButton<TValue>[];
    menuToState(menu: TValue[]): TState;
    onActiveButton(ctx: TCtx, activeButton: MenuOption<TValue>): void;
    formatButtonLabel(ctx: TCtx, button: KeyboardButton<TValue>): string;
}
