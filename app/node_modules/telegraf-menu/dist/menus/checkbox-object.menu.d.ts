import { CheckboxConfig, DefaultCtx } from '../interfaces';
import { KeyboardButton } from '../keyboard-button';
import { CheckboxMenu } from './checkbox.menu';
export declare class CheckboxObjectMenu<TCtx extends DefaultCtx = DefaultCtx, TState extends Record<TValue, boolean> = Record<string, boolean>, TValue extends string = string> extends CheckboxMenu<TCtx, TState, TValue> {
    private genericConfig;
    constructor(config: CheckboxConfig<TCtx, TState, TValue>);
    stateToMenu(state: TState): KeyboardButton<TValue>[];
    menuToState(menu: TValue[]): TState;
}
