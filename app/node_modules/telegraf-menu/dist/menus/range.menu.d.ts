import { GenericMenu } from '../generic-menu';
import { DefaultCtx, MenuOption, RangeConfig, RangeState } from '../interfaces';
import { KeyboardButton } from '../keyboard-button';
export declare class RangeMenu<TCtx extends DefaultCtx = DefaultCtx, TState extends RangeState<TValue> = RangeState<any>, TValue extends string = string> extends GenericMenu<TCtx, TState, TValue> {
    private config;
    constructor(config: RangeConfig<TCtx, TState, TValue>);
    stateToMenu(state: TState): KeyboardButton<TValue>[];
    menuToState(menu: TValue[]): RangeState;
    onActiveButton(ctx: TCtx, activeButton: MenuOption<TValue>): void;
    formatButtonLabel(ctx: TCtx, button: KeyboardButton<TValue>): string;
    private getRangeButtonIndexes;
}
