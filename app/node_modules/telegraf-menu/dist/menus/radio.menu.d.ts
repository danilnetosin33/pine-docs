import { GenericMenu } from '../generic-menu';
import { DefaultCtx, MenuOption, RadioConfig } from '../interfaces';
import { KeyboardButton } from '../keyboard-button';
export declare class RadioMenu<TCtx extends DefaultCtx = DefaultCtx, TState extends string = string, TValue extends string = string> extends GenericMenu<TCtx, TState, TValue> {
    private config;
    constructor(config: RadioConfig<TCtx, TState, TValue>);
    stateToMenu(state: string): KeyboardButton<TValue>[];
    menuToState(menu: TValue[]): TValue;
    onActiveButton(ctx: TCtx, activeButton: MenuOption<TValue>): void;
    formatButtonLabel(ctx: TCtx, button: KeyboardButton<TValue>): string;
}
