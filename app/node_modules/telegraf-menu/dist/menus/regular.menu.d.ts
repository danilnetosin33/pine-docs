import { GenericMenu } from '../generic-menu';
import { DefaultCtx, MenuOption, RegularMenuConfig } from '../interfaces';
import { KeyboardButton } from '../keyboard-button';
export declare class RegularMenu<TCtx extends DefaultCtx = DefaultCtx, TValue extends string = string> extends GenericMenu<TCtx, string, TValue> {
    private config;
    constructor(config: RegularMenuConfig<TCtx, TValue>);
    stateToMenu(): any[];
    menuToState(menu: TValue[]): string;
    onActiveButton(ctx: TCtx, activeButton: MenuOption<TValue>): void;
    formatButtonLabel(ctx: TCtx, button: KeyboardButton<TValue>): string;
}
