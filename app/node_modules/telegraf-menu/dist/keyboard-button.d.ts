export declare class KeyboardButton<TValue extends string = string> {
    label: string;
    value: TValue;
    isDefault: boolean;
    constructor(label: string, value: TValue, isDefault?: boolean);
}
